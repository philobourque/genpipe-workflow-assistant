name: tests

# Three suites, chosen because they are the ones that can run anywhere and still
# say something true. They import only the standard library -- gate_rules.py,
# runs.py and fakecluster.py were deliberately kept free of biomni for exactly
# this reason, so CI needs no pinned agent stack, no API key, and no cluster.
#
# What is NOT here, and why: tests/test_lifecycle.py and tests/test_app.py drive
# the real LangGraph gate and a real terminal, so they need biomni==0.0.8 and its
# pinned langgraph. Installing that on every push to check a gate matcher would
# trade a two-second signal for a several-minute one. Those two run on the
# cluster, in the venv, before a release -- see the README.

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  offline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          # Matches the version start_agent.sh loads on the cluster, so a syntax
          # or stdlib difference shows up here rather than on Rorqual.
          python-version: "3.12"

      - name: The gate's invariants
        # First, and on its own step, because it is the only test here whose
        # failure means something unsafe rather than something broken.
        run: python tests/test_gate_rules.py

      - name: Runs and jobs
        run: python tests/test_runs.py

      - name: The fake cluster
        run: python tests/test_fakecluster.py

      - name: Every renderer
        run: python tests/test_display.py

      - name: Everything imports without a cluster present
        # A stdlib-only module that quietly grows a heavy import would break CI
        # in a confusing way later; catching it as its own step names the cause.
        run: python -c "import gate_rules, runs, fakecluster, display; print('ok')"
